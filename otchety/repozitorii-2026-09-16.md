# Отчёт: репозитории, CLAUDE.md, PRICE.md, DOCS.md, README-zapusk (задачи 38–43), 16.09.2026

**Вывод одной строкой: оба репозитория собраны локально на Mac (`~/elg/elg-pzz`, 963 файла, 173 МБ; `~/elg/elg-docs`, 40 файлов) и проверены клонированием в пустой каталог, где прошли все самопроверки, отчёт продукта, имитация сбора и справка; на флешке остались сырьё 65 ГБ, профили Chrome и копия всего остального; в GitHub ничего не отправлено, потому что репозитории создаёт Игорь, а на Mac нет ни `gh`, ни сохранённых учётных данных.** Всё ниже с пометками.

## 1. Что переехало

| Куда | Что | Откуда | Размер |
| --- | --- | --- | --- |
| `elg-pzz` (приватный) | `scripts/` (46 скриптов), `out/` без `*.md` верхнего уровня (индексы, профили, справки, отчёты продукта, тексты, снимки НСПД), `tools/` (три сторонних конвертера как обычные файлы), `ca/` (отпечаток сертификата НСПД), `README.md`, `README-zapusk.md`, `.gitignore` | флешка `pzz/` | 963 файла, 311 МБ рабочего дерева, `.git` 173 МБ; самый большой файл `krt_index.csv` 24 МБ, файлов >50 МБ нет, Git LFS не нужен [Certain] |
| `elg-docs` (публичный) | `CLAUDE.md`, `PRICE.md`, `DOCS.md`, `README.md`; `tz/` (v3.4; v3.0 и v3.2 в `archive/`); `kanon/`; `zadaniya/` (4 файла); `otchety/` (22 отчёта, сводки, сборник); `status/` (3); `teksty/` (4) | Downloads Игоря с 02.09, `out/*.md` с флешки, корень флешки | 40 файлов, 568 КБ |

Скрипты переведены с жёсткого пути флешки на корень репозитория (`P = каталог выше scripts/`, переопределяется `PZZ_ROOT`): 13 рабочих скриптов; 30 зондов и разовых инструментов путь флешки сохранили, они помечены в README как история [Certain]. Дата индекса теперь читается из `out/krt_index/index_date.txt` (07.09.2026), а не из времени файла: при копировании время менялось, и отчёт из клона напечатал бы «собран 16.09» на данных 07.09; конвейер пишет этот файл при `merge`.

## 2. Что осталось на флешке и почему

| Что | Почему не в git |
| --- | --- |
| `pzz/raw/` — 65 ГБ: 37 465 файлов актов 120-ПП, 11 127 файлов актов о КРТ/ППТ/изъятии, 3 228 документов ДГИ, книги ПЗЗ, нормативные файлы | по заданию сырьё в git не кладётся; в `.gitignore`, в README-zapusk раздел 6 — где лежит и как пересобрать индексы |
| `pzz/out/nspd*/chrome-profile/` — профили headless Chrome (кэши, 37 МБ модель) | создаются при запуске, в `.gitignore` |
| `pzz/out/*.md` — 21 отчёт | переехали в `elg-docs/otchety/`, в приватном не дублируются |
| Всё остальное | копия на дату 16.09.2026; рабочее место теперь репозитории |

Ничего с флешки не удалено: по заданию удаление только после проверки обоих репозиториев клонированием с GitHub, а туда ещё не отправлено.

## 3. Чего нет в репозиториях

- **ТЗ v3.5**: Игорь ссылается на неё в задании, файла нет ни в Downloads, ни на флешке; действующим на диске стоит v3.4, это записано в `DOCS.md` [Certain].
- **`oferta-ekspress-razbor.md`**: опубликована на сайте, локальной копии нет [Certain].
- **Задания 04.09, 07.09 (два), 08.09, 09.09**: текст только в чате; в `zadaniya/` лежат 02.09, два от 11.09 (восстановлены дословно из переписки) и 16.09.
- **`.docx`-версии ТЗ v1.0 и v2.0**: остались в Downloads, не Markdown, история до v3.
- Тексты по лидогенерации от 16.09 (`plan-lidy-*`, `soobshchenie-rieltoru`, `rieltor-spb-1`) — продукт другого разговора, не Градпрофиль; в `elg-docs` не переносились, решение за Игорем.

## 4. Проверка на клиентские данные в публичном репозитории

Скан по кадастровым номерам и именам: в публичной копии остались только пилотные участки с торгов (77:06:0012020:18277, 77:03:0010007:5381/5896, 77:01:0001091:3555), номера из открытых актов (77:02:0006002:11957, 77:05:0000000:1031, соседи из ППТ) и синтетический 77:99:0000000:1. Раздел 4.7 сборника по объекту заказчика и одно упоминание в сводке заменены на пометку «объект заказчика, в приватном репозитории»; бумажные выписки по второму объекту заказчика и их расшифровка — только в `elg-pzz/out/profiles/`. `config_ispolnitel.json` — только в приватном [Certain].

## 5. Проверка README-zapusk из чистого каталога [Certain]

`git clone` локального `elg-pzz` в пустой каталог, затем по README: `snesut.py --selftest` (9 проверок), `spravka.py --selftest` (7), `egrn_xml.py --selftest` — проходят; `snesut.py 77:03:0010007:5896` собрал отчёт (🟡); `MOCK=1 profile.py` собрал профиль за 3,5 с; `spravka.py` собрала справку; `krt_lookup` нашёл 2 акта по 77:05:0000000:1031 с датой индекса 07.09.2026; `raw/` в клоне отсутствует. Живое окно с НСПД по README не повторялось: сеть в момент проверки была за VPN, рецепт окна в README тот, что сработал 11 и 16 сентября.

## 6. Что нужно от Игоря

1. Создать на GitHub `elg-docs` (public) и `elg-pzz` (private), выдать доступ на запись, прислать адреса. Затем одна команда на каждый репозиторий: `git remote add origin <адрес> && git push -u origin main`; на Mac нет `gh` и сохранённого токена, так что первый пуш пройдёт с вашим вводом логина и токена в окне Git, либо сделайте его сами из `~/elg/`.
2. Решить, допустим ли публичный `elg-docs` с методикой. Если нет — оба приватные, документы в проект чата грузятся руками.
3. Утвердить или поправить `PRICE.md`; блок «Что нужно решить» ниже.
4. Положить ТЗ v3.5 в `tz/`, если она существует.
5. После проверки клонов с GitHub — флешка становится копией; удалять с неё ничего не требуется.

## 7. Что нужно решить (из PRICE.md, не решено)

1. Градпрофиль-экспресс: 9 900 ₽ (сводка 16.09) или 20–30 тыс. ₽ (ТЗ v3.4/3.5, SKU 1а).
2. Градпрофиль-полный: 25 000 ₽ или «1а + 15–25 тыс.» по ТЗ (35–55 тыс.).
3. Сайт: экспертиза 30 000, аудит от 30 000, экспресс-разбор 5 000 — как соотносятся с линейкой и включать ли экспресс-разбор в прайс.
4. Аудит: 80–150 тыс. ₽ в ТЗ против «от 30 000 ₽» на сайте.
5. Пакетные цены 1 900 / 2 900 ₽ для агентств — предложение, не обсуждалось.
6. SKU 1б не выпускается до живой электронной выписки; заказывать ли выписку по пилоту за 580 ₽.
7. НДС и налоговый режим в оферте.
8. Наблюдение за объектом и аудит перед покупкой из ТЗ — остаются ли в линейке.

## 8. Предложения к ТЗ и прайсу

Цен и ТЗ при переносе не менял. Одно предложение: в ТЗ v3.4 нет продукта «Проверка на изъятие и КРТ» (SKU 1а/1б прайса), а в прайсе нет SKU 3 и 4 из ТЗ; при следующей редакции ТЗ линейку и прайс стоит свести в одну таблицу с одной нумерацией.
